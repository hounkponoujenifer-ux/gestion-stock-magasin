#include <iostream>

using namespace std;

int main()
{
    int stock;
    int SeuilAlerte = 20;
    int SeuilCommande =10;
    int achats, commande, jours = 30;
    cout << "Stock initial" << endl;
    cin>>stock;
    for (int j = 1; j<=jours; j++){
        achats = rand()%15+1;
        if ( achats > stock){
            achats = stock;
        }
        stock -= achats;
        if(stock<SeuilCommande &&commande ==0){
            commande = 50;
    }
    if (commande>0){
        stock += commande;
        commande = 0;
        cout<<"Jour" <<j <<"|stock : " <<stock <<"|Achats : " <<achats <<endl;
        if (stock<SeuilAlerte){
            cout<<"Alerte : Stock faible ! " <<endl;
        }
    }
    return 0;
    }
}
